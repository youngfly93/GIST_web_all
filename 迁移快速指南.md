# 🚀 DBGist 域名迁移快速指南

## 一、京东云 DNS 配置

登录 [京东云控制台](https://console.jdcloud.com/) → 域名服务 → DNS 解析

添加两条 A 记录：

```
类型: A    主机记录: www    记录值: [您的服务器IP]    TTL: 600
类型: A    主机记录: @      记录值: [您的服务器IP]    TTL: 600
```

**获取服务器IP:**
```bash
curl ifconfig.me
```

---

## 二、HTTP 访问配置（必做）

```bash
cd /home/ylab/GIST_web_all
bash setup_dbgist_domain.sh
```

**等待完成后访问：**
- http://www.dbgist.com
- http://dbgist.com

---

## 三、HTTPS 访问配置（推荐）

**等待 DNS 生效 5-10 分钟后执行：**

```bash
cd /home/ylab/GIST_web_all
bash setup_dbgist_ssl.sh
```

**完成后访问：**
- https://www.dbgist.com ✨
- https://dbgist.com ✨

---

## 检查服务状态

```bash
# 检查关键服务
curl http://127.0.0.1:5174  # 前端
curl http://127.0.0.1:8000  # 后端

# 检查 nginx
sudo systemctl status nginx

# 查看端口
sudo ss -tlnp | grep -E ":(80|443|5174|8000)"
```

---

## 如果遇到问题

**DNS 解析检查：**
```bash
ping www.dbgist.com
```

**nginx 日志：**
```bash
sudo tail -f /var/log/nginx/error.log
```

**重启 nginx：**
```bash
sudo systemctl restart nginx
```

---

## 完整文档

详细步骤和故障排查请查看：
- `DOMAIN_MIGRATION_GUIDE.md`

---

**注意事项：**
- ✅ 原 81 端口配置已自动备份，可随时恢复
- ✅ SSL 证书有效期 90 天，已配置自动续期
- ✅ 所有路由和功能保持不变




